# link-api

This directory contains the generated go-zero HTTP API skeleton.

The current API service exposes only:

- `GET /healthz`
- `GET /readyz`

Stage 3 foundation work has added database schema/model groundwork in `link-rpc`, but business HTTP routes are still reserved for the next Stage 3 implementation pass.
