# API Contracts

This directory contains go-zero `.api` files.

The current API contract includes only health/readiness routes. Stage 3 database foundation exists, but business API contracts are reserved for the next Stage 3 implementation pass.
